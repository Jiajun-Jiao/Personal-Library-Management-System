<!doctype html>
<html>
<head>
	<meta charset="utf-8">
	<title></title>
	<link rel="stylesheet" href="/css/site.css" type="text/css" media="screen" title="no title" charset="utf-8">
</head>

<body>
	<div id="outer">
		<header>
			<nav>
				<span class="home-link"><a href="/">Home</a> {{#if user}}| <a href="/booklist/add">Add Booklist</a>{{/if}}</span>
				<span class="login-status">
					{{#if user}}
					Logged in as: {{user.username}}
					{{else}}
					<a href="/register">Register</a> or <a href="/login">Login</a>
					{{/if}}
				</span>
			</nav>
		</header>

		<h1 class="head">Personal Library Management System</h1>
		<br>
		<div class="content">
			{{{ body }}}
		</div>
	</div>
</body>
</html>
